name: wiseclient
description: A http client based on Dio with extra features.
version: 1.0.0

environment:
  sdk: ^3.1.3

dependencies:
  dio: ^5.3.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
